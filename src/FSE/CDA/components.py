from elements import Attribute, Sub, Value

class ClinicalDocument:
    """ Clinical Document Class """
    def __init__(self):
        # ATTRS
        #self.xsi = Attribute("xsi", {}, False)

        # ELEMENTS
        #self.sub = Sub(Prova, "ciao", {}, False)
